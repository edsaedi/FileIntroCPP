#include <iostream>
#include <fstream>
#include <string>
#include <string_view>
#include <vector>


int main()
{
	/*std::ofstream file("test.txt");


	if (file.is_open())
	{
		for (size_t i = 0; i < 100; i++)
		{
			file << i << "\n";
		}
	}

	file.close();


	std::ifstream openedfile("test.txt");

	std::vector<std::string> lines;

	if (openedfile.is_open())
	{
		std::string line;

		while (std::getline(openedfile, line))
		{
			lines.push_back(line);

		}
	}
	else
	{
		std::cout << "Could not load the file test.txt" << std::endl;
	}

	std::cout << "Read back from the file" << std::endl;

	for (auto temp : lines)
	{
		std::cout << temp << std::endl;
	}*/

	// create a class called File which has two functions
	// ReadAllLines which returns a vector of strings
	// WriteAllLines which returns a bool status and takes in a vector to be written to a file



	return 0;
}

